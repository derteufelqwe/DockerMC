<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%style{%d{ISO8601}}{white} %highlight{[%-5level]}{INFO=blue} %highlight{%msg%n%throwable}{INFO=def, TRACE=def, DEBUG=def}" disableAnsi="false"/>
        </Console>

        <RollingFile name="RollingFile"
                     fileName="./logs/ServerManager-log4j2.log"
                     filePattern="./logs/$${date:yyyy-MM}/ServerManager-log4j2-%d{-dd-MMMM-yyyy}-%i.log.gz">
            <PatternLayout>
                <pattern>%d [%-5level] %msg%n%throwable</pattern>
            </PatternLayout>
            <Policies>
                <!-- rollover on startup, daily and when the file reaches
                    10 MegaBytes -->
<!--                <OnStartupTriggeringPolicy />-->
                <SizeBasedTriggeringPolicy
                        size="10 MB" />
<!--                <TimeBasedTriggeringPolicy />-->
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- LOG everything at INFO level -->
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>

</Configuration>